version: '3.8'

services:
  nuxt-app:
    image: seein-prydia:latest
    build:
      context: .
      args:
        ENV_PROFILE: ".env.dev"
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped